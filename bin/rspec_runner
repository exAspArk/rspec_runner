#!/usr/bin/env ruby

cmd = ARGV[0]

case cmd
when 'monitor'.freeze
  require 'rspec_runner/runner'
  RspecRunner::Runner.run
when 'start'.freeze
  require 'rspec_runner/server'
  RspecRunner::Server.run
else
  require 'rspec_runner/client'
  RspecRunner::Client.execute(cmd)
end
